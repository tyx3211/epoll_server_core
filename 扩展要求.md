设计一个简单的博客网站，至少包含index.html，. gif, .jpeg，.css，.js，.ico文件，且有多级目录，如图片放在img文件夹，css文件放在css文件夹。目录层级示例如图所示：
当用户请求http://localhost:9001和http://localhost:9001/index.html 时返回index.html页面
修改http server，使其能够作为web服务器返回博客的内容：
解析HTTP请求的文件扩展名
根据文件扩展名确定对应的MIME类型
从服务器文件系统中读取请求的静态资源文件
发送HTTP响应，包含静态资源文件的内容
设计文件不存在等错误处理函数，并返回不同的状态码

